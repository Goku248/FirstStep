/**
 * if header file should be shared to user
 * they should be put in `FirstStep/include` directroy
 *
 * header file should have extension of `.hpp`
 */

#pragma once

int add(int, int);

// length of c_str
int length_str(const char *c_str);